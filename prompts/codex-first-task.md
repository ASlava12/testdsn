Read `AGENTS.md`, `IMPLEMENT.md`, and the files under `spec/`.

Goal:
Create the repository skeleton exactly as described in `docs/REPO_LAYOUT.md`.

Constraints:
- do not implement protocol logic yet;
- keep code compiling where practical;
- add placeholder modules and TODO markers only where the spec is still intentionally incomplete;
- do not invent extra features.

Validation:
- run `cargo check --workspace`
- report what still cannot build and why
